const isValidYear = (value) => {
  console.log(typeof value);
  return true;
};

const isValidMonth = (value) => {
  console.log(typeof value);
  return true;
};

const isValidDay = (value) => {
  console.log(typeof value);
  return true;
};

module.exports = {
  isValidYear,
  isValidMonth,
  isValidDay,
};
